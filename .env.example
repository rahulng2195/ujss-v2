# Email Configuration
# SMTP settings for sending emails from rahul@crezvatic.com

# SMTP Host (e.g., smtp.gmail.com, smtp.office365.com, or your hosting provider's SMTP)
SMTP_HOST=smtp.gmail.com

# SMTP Port (587 for TLS, 465 for SSL)
SMTP_PORT=587

# Set to true for port 465 (SSL), false for port 587 (TLS)
SMTP_SECURE=false

# Your email address (sender email)
SMTP_USER=rahul@crezvatic.com

# Your email password or app-specific password
# For Gmail: Use App Password from https://myaccount.google.com/apppasswords
# For other providers: Use your email password or app-specific password
SMTP_PASSWORD=your_email_password_here
